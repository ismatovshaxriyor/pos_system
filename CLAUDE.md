# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

A restaurant POS system built around an offline-first "Ona-Bola" (Mother-Child / Cloud-Local) architecture. It is **two independent Django projects** living side by side in this repo, each with its own Docker stack, database, and requirements file — there is no shared code between them:

- **`local_server/`** — the "Bola" (Child). Runs on a machine physically inside each restaurant (mini PC/laptop). Handles day-to-day POS operations (orders, tables, products, cashiers/waiters), enforces the license kill-switch, and must work fully offline for days at a time. Django apps: `core` (POS CRUD), `licensing` (activation/JWT/kill-switch/heartbeat/remote commands).
- **`cloud_server/`** — the "Ona" (Mother). A single central server that all restaurants report to. Issues license JWTs, receives heartbeats/metrics, and pushes remote commands (block, unblock, force-renew, update). Django apps: `tenants` (Restaurant/License/RemoteCommand + admin control panel), `sync` (the HTTP API Bola servers talk to).

The full design intent is documented in Uzbek under `docs/` — read the relevant doc before touching sync, licensing, or deployment code:
- `docs/1_ona_bola_arxitekturasi.md` — cloud/local architecture and two-way sync engine design
- `docs/2_xavfsizlik_va_litsenziyalash.md` — code protection (Cython/PyArmor), hardware fingerprinting, license kill-switch, Docker hardening
- `docs/3_tizim_optimizatsiyasi.md` — DB indexing, connection pooling, Redis caching, Celery concurrency limits, Nginx/static file strategy
- `docs/4_amaliy_ish_oqimi_va_deployment.md` — new-restaurant onboarding flow, Cloudflare Tunnel remote access, disaster recovery, MVP phasing

**Note:** the docs describe the license flow as a plain hardware-hash check; the actual implementation upgrades this to short-lived RS256 JWTs per docs/2's own "signed token with kill-switch" intent — see Licensing architecture below for what's actually built.

## Commands

Each server is developed and run independently via its own compose file. There is no root-level build/run command. Two compose files per project: `docker-compose.yml` (dev — `runserver`, source bind-mounted, ports open on `127.0.0.1`) and `docker-compose.prod.yml` (prod — registry image, Watchtower, no db/redis ports published, needs a real `.env`). The prod app server differs between the two: `cloud_server` runs plain `gunicorn config.wsgi:application`, while `local_server` runs `daphne config.asgi:application` instead (ASGI, needed to serve the `ws/events/` WebSocket channel — see `local_server/core/CLAUDE.md`); its dev `runserver` is also ASGI-aware for the same reason, since `daphne` is first in `INSTALLED_APPS`.

**Migration files ARE committed to git.** This deliberately reverses the original "gitignore migrations, regenerate per environment" strategy: that scheme freezes a prod DB's schema after the first deploy (a freshly regenerated `0001_initial` is recorded as already applied, so every later model change silently never lands) — full post-mortem in `future-docs/03_migratsiya_strategiyasi.md`. `entrypoint.sh` in both projects now runs only `migrate --noinput` when `RUN_MIGRATIONS=1`; containers never run `makemigrations`. So whenever you change a `models.py`: run `makemigrations` in the dev container and **commit the generated files in the same change** — prod applies exactly what's in git, nothing more.

**A prod DB provisioned before this strategy** (mismatched migration history vs. actual schema — this bit the cloud prod with a 502 on 2026-07-11) needs a one-time fake-migration fix — see the `fix-legacy-migration-state` skill.

### local_server (dev: port 8000, Postgres 127.0.0.1:15432, Redis 127.0.0.1:16379)
See `local_server/CLAUDE.md` for the dev command set (compose up, migrate, test, `activate_license`) and admin/Swagger URLs.

### cloud_server (dev: port 8001, Postgres 127.0.0.1:25432, Redis 127.0.0.1:26379)
See `cloud_server/CLAUDE.md` for the dev command set (compose up, migrate, test, `generate_signing_keys`) and admin/Swagger URLs.

Both servers read config from environment variables with dev fallbacks in `config/settings.py`. `.env.example` exists in each project root — copy to `.env` and fill in secrets before any prod deployment (prod compose requires a real `.env` via `env_file:`). Settings-level gotchas: both servers enable DRF-wide default throttling (`anon` 10/minute, `user` 100/minute), so a script hammering an endpoint will see 429s that have nothing to do with the PIN rate-limiter described below; `local_server` refuses to boot (`ValueError` at settings import) when `DEBUG=False` and `ONA_SERVER_URL` isn't `https://`. Under `manage.py test` both settings files disable throttling entirely (classes *and* rates — `ActivationView` declares `throttle_classes` at the view level, which empty default classes alone wouldn't touch) and move the Redis cache to DB `/9`: Django creates a throwaway test Postgres but *not* a test Redis, so without the swap, tests and the dev stack share throttle counters, PIN rate-limit keys, and `restaurant_metrics_*` liveness keys — a nondeterministic-failure factory.

Both prod composes are hardened: `depends_on` is healthcheck-gated (Postgres `pg_isready`, Redis `ping`), and the app containers run with a read-only root filesystem (`read_only: true`, `tmpfs` only for `/tmp` and `/run`) — code that writes anywhere outside `/app/media` (a named volume) or `/tmp` will work in dev and fail in prod until the compose file changes too. Two places already had to accommodate that: `celery_beat` runs with `--schedule=/tmp/celerybeat-schedule` (its default schedule file lives in `/app` → boot crash-loop under `read_only`), and `cloud_server`'s `web` adds `/app/staticfiles` to `tmpfs` (its `entrypoint.sh` runs `collectstatic` on every start — required by the whitenoise manifest storage). `cloud_server`'s prod compose also gained the same daily `db_backup` service (`prodrigestivill/postgres-backup-local`, 14-day retention) that `local_server` already had.

### First-time licensing setup (both servers, needed before activation/kill-switch will work)
One-time RSA keypair generation/placement across both servers — see the `setup-licensing` skill.

`local_server/pos-bola.avahi.service` advertises the Bola over mDNS on the restaurant WiFi (`_pos-bola._tcp` on port 8000, `path=/api/` TXT record) so the Flutter staff apps can find it without a static IP/DHCP reservation — see `docs/4_amaliy_ish_oqimi_va_deployment.md` for install steps; not wired into any compose service, it's installed on the host.

## Architecture

### `local_server/core` — POS domain, kitchen printing, attendance, staff auth, WebSocket events
See `local_server/core/CLAUDE.md` (loads automatically when working under that directory): the sync-ready `BaseModel`/`sync_uuid` design, order-creation idempotency, `django-simple-history` wiring, the full `Order`/`OrderItem`/`Payment` lifecycle (computed totals, split payments, discounts), media/image URL handling, kitchen ticket printing & routing, staff attendance/geofencing, the three staff login paths (admin password / waiter TOFU / PIN+device) and device revocation, and the `ws/events/` real-time channel.

### Licensing & kill-switch (`local_server/licensing/`, `cloud_server/tenants/` + `cloud_server/sync/`)

**JWT signing is asymmetric (RS256):** Ona holds `LICENSE_PRIVATE_KEY` and is the only thing that can mint tokens (`cloud_server/sync/jwt_utils.py::issue_license_token`). Bola holds only `LICENSE_PUBLIC_KEY` and verifies tokens fully offline (`local_server/licensing/jwt_utils.py::verify_token`), which is what lets the kill-switch work for days without internet.

**Bola's state is a DB singleton**, not a file: `licensing.models.LicenseState` (pk forced to 1). It survives container recreation via the Postgres volume and is shared by the `web`/`celery_worker`/`celery_beat` containers. Every `.save()` invalidates the Redis-cached verdict key (`license:verify`) so blocks/unblocks take effect on the next request rather than waiting out the cache TTL.

**Activation flow:** `POST /api/license/activate/` (Bola, unauthenticated) → `OnaClient.activate()` → `POST /api/sync/activate/` (Ona, unauthenticated, license key in body). Ona binds the posted `hardware_hash` to the `License` row on *first* activation and rejects any different hash afterward (`"Qurilma mos kelmadi."`) — re-activating on new hardware requires an admin to run the `reset_hardware_hash` action on `LicenseAdmin` first (disaster-recovery path from docs/4). `ActivateView` guards `response.json()` with a `try`/`except ValueError` → `502`: a reverse proxy/tunnel sitting in front of Ona (e.g. cloudflared) can return an HTML error page instead of JSON on an outage, and without the guard that surfaced as an unhandled 500 on the Bola side instead of a legible "try again later" message.

**Long-offline survival via pre-issued token batches:** `cloud_server/sync/jwt_utils.py::issue_license_token_batch` (called by both activate/renew in `cloud_server/sync/views.py`) signs several consecutive short-lived JWTs per call instead of one — each covering `LICENSE_TOKEN_TTL_DAYS`, chained back-to-back, capped so none extends past `License.expires_at` — and returns all of them; Bola stores the extras in `LicenseState.pending_tokens` (JSONField) and rotates through them locally via `LicenseState.current_valid_token()` as the active one expires, verifying purely against the local public key with zero calls back to Ona. This is what lets a restaurant go weeks without a successful heartbeat and keep running. For the rarer case of a restaurant that exhausts the whole batch (or never got one), `LicenseAdmin`/`RestaurantAdmin`'s `generate_offline_token` action mints one single `issue_license_token()` (not a batch, and only for a license whose `hardware_hash` is already bound — this is a *renewal* path, not an activation one) meant to be copied — not dictated over a phone call, it's long — to the restaurant via Telegram/SMS; they apply it at `POST /api/license/apply-offline-token/` (`local_server/licensing/views.py::ApplyOfflineTokenView`, unauthenticated, fully offline verification against the local public key, no Ona round-trip). A successful apply also clears `blocked_reason == 'license_inactive'`, so this doubles as a manual, no-internet unblock.

**License key format:** `tenants.models.generate_license_key()` produces a human-typeable `XXXX-XXXX-XXXX` key (12 chars from the same confusion-free alphabet as `local_server/core/services.py::CODE_ALPHABET` — no `0`/`O`/`1`/`I`), built for a mobile "type it or scan a QR" activation screen — not the old `secrets.token_hex(20)` 40-char format (pre-existing `License` rows keep whatever they were originally issued, both formats coexist fine since it's just a default, not a validated pattern). Two related case-sensitivity fixes ship with this: `ActivationView.post()` (`cloud_server/sync/views.py`) looks the key up with `key__iexact`, and its response echoes back the *canonical* DB-cased key (`license_key`) — `local_server/licensing/views.py::ActivateView` stores *that* into `LicenseState.license_key`, not the user-typed string, because that same value is reused verbatim as the `Authorization: Token <key>` credential on every subsequent renew/heartbeat/error-log call, and `LicenseAuthentication`'s lookup there is intentionally still case-sensitive (exact `key=`) — normalizing case only at activation and not at every later authenticated call would activate fine and then silently break the very next heartbeat. `LicenseAdmin.qr_code` (`cloud_server/tenants/admin.py`) renders the key as a PNG QR code (`qrcode` + `Pillow`, embedded as a `data:` URI — no extra view/URL, and no new unauthenticated surface since it only ever renders inside the already-staff-gated admin page) plus a "QR kodni yuklab olish" download link using that same data URI, so an operator onboarding a restaurant can save/share the image (e.g. via Telegram) for the admin's phone to scan instead of typing the key. PNG rather than SVG deliberately — most chat apps render/preview PNG inline but not SVG.

**Hardware fingerprinting** (`local_server/licensing/hardware.py::get_hardware_fingerprint`) tries, in order: `HARDWARE_ID_OVERRIDE` env (dev/test only) → `/sys/class/dmi/id/product_uuid` → `/etc/machine-id` → a real burned-in MAC via `uuid.getnode()`. **Important:** the MAC fallback explicitly rejects both the multicast bit *and* the locally-administered bit (`first_octet & 0x02`) — Docker's default bridge network hands out `02:xx:xx:xx:xx:xx` MACs that silently change on every container recreate, which would otherwise re-brick the license on every restart/update. This was caught by live-testing, not by inspection — if you touch this function, re-verify by recreating the container and confirming `/api/license/status/` still reports `activated: true` afterward. Docker Desktop containers typically have *neither* `product_uuid` nor `machine-id` available, so local dev **requires** `HARDWARE_ID_OVERRIDE` set to a stable string in `docker-compose.yml` (already done: `dev-local-machine`).

**Kill-switch middleware** (`local_server/licensing/middleware.py::LicenseEnforcementMiddleware`, registered in `MIDDLEWARE` before `CsrfViewMiddleware`): gates every `/api/` request except `/api/license/*` (activation/status must stay reachable) and `/admin/` (a manager needs to see state and re-activate). Blocked → `402 {"detail": "Tizim bloklandi. To'lovni amalga oshiring."}`. Controlled by `LICENSE_ENFORCEMENT` (`0` in dev compose, should be `1` in prod). Verdict is cached in Redis: 60s when OK, only 10s when blocked (so unblocks propagate fast). On a DB error (e.g. mid-migration) it fails open under `DEBUG`, fails closed otherwise.

**Heartbeat & lenient auth** (`cloud_server/sync/authentication.py::HeartbeatAuthentication`, a subclass of `LicenseAuthentication` that skips the `is_active`/`expires_at` checks): the heartbeat/command-result endpoints deliberately accept a *dead* license — otherwise a deactivated restaurant would go dark instead of receiving the "you're blocked" signal. Only `activate`/`renew` use the strict `LicenseAuthentication` (never issues tokens for a dead license). Bola's `licensing.tasks.send_heartbeat` (Celery beat, every 60s, `expires=50`) posts `licensing.metrics.collect_metrics()` (psutil CPU/RAM/disk + `APP_VERSION` + unsynced order count) and reads back `license_active` (self-block/unblock) and any queued `commands`.

**Remote commands** (`tenants.models.RemoteCommand`, Ona-side queue): since Bolas sit behind NAT, commands are never pushed — they piggyback on the heartbeat *response* (`pending` → `sent`, capped at 10 per beat) and Bola reports outcomes to `POST /api/sync/commands/<uuid>/result/`. Dispatch table lives in `local_server/licensing/tasks.py::COMMAND_HANDLERS` (`block_system`, `unblock_system`, `force_license_renew`, `force_sync` stub, `restart_services` — deliberately refuses, no docker.sock access). `update_app` is special-cased outside that table: it must report its "started" result *before* calling Watchtower, because Watchtower recreates the very `celery_worker` container running the task; actual rollout success is confirmed on Ona by the next heartbeat's `app_version`, not by the command result.

**Update rollout** (Phase 5, prod-only): `local_server/docker-compose.prod.yml` runs a `watchtower` container in HTTP-API mode (`WATCHTOWER_HTTP_API_UPDATE`, token-protected, no polling) scoped to the `web`/`celery_worker`/`celery_beat` services via `com.centurylinklabs.watchtower.enable=true` labels (so `db`/`redis`/`db_backup` are never touched). Images come from `${POS_IMAGE:-ghcr.io/ORG/pos-local}:${RELEASE_CHANNEL:-stable}` — Watchtower re-pulls whatever tag a container already runs, so all restaurants on the same `RELEASE_CHANNEL` update together; per-restaurant pinning would need a distinct channel tag per machine (already parameterized, just unset today). `entrypoint.sh` runs `migrate --noinput` only when `RUN_MIGRATIONS=1` (set on `web` only, so three containers don't race migrations after Watchtower recreates them). **Cutting a release / verifying rollout:** the reported version (`APP_VERSION`, sent in every heartbeat's metrics) is now read from `local_server/VERSION` (a file baked into the image, `config/settings.py::_read_app_version`) — deliberately *not* from an env var anymore, because a prod `.env` that pinned `APP_VERSION` would keep reporting the old number after Watchtower swapped the image, silently breaking rollout verification (env is only a fallback when the file is unreadable). So a release is three steps, now mostly automated: **(1) build+push the image** — bump `local_server/VERSION`, then push a `v*` git tag (e.g. `v0.1.2`), which fires `.github/workflows/release-local-server.yml` (the *only* CI that builds the local_server/Bola image — the earlier "no local_server CI" note is now stale): it builds the prod image and pushes it to `ghcr.io/<owner>/pos-local:stable` **and** `:<VERSION>` using the built-in `GITHUB_TOKEN` (no extra secret; package is private by default, so each Bola host needs a one-time `docker login ghcr.io` — Watchtower already mounts `~/.docker/config.json`). A `v*` tag push does **not** trigger the cloud deploy (that's filtered to `push: branches: main`). **(2) tell the fleet** — one Ona admin action, `tenants/admin.py::action_release_version` ("⬆️ Tanlangan Bolalarga YANGI VERSIYA yuborish"): select the restaurants, type the version once on the intermediate page, and it sets `desired_version` **and** enqueues `update_app` for every *active* one at once (inactive/blocked restaurants are skipped). The older `action_update_app` (per-restaurant, reads each `desired_version`, blank-skips) is kept for backward-compat. **(3) verify** — rollout is confirmed when a restaurant's heartbeat-reported `app_version` (shown by `RestaurantAdmin.app_version_display`) converges to `desired_version`. Note the whole path only works for a Bola running the **prod** compose (Watchtower lives there, not in dev `docker-compose.yml`); a dev/git-pull machine has no Watchtower and can't receive `update_app`.

**Code protection for the kill-switch itself (Cython, not PyArmor):** `local_server/Dockerfile` is a multi-stage build — stage 1 (`builder`) compiles the license-critical modules (`licensing/middleware.py`, `jwt_utils.py`, `hardware.py`, `client.py`, `tasks.py`, `views.py`) to `.so` via `licensing/setup.py build_ext --inplace`, then deletes those `.py` sources (plus the `.c`/`build/` intermediates and `licensing/tests/`) before stage 2 copies the result into the final image — a shipped prod image has no readable source for the enforcement logic itself, only compiled extensions. `licensing/models.py`, `migrations/`, `urls.py`/`apps.py`/`admin.py`/`serializers.py` are deliberately left as plain `.py` (Django introspects them at runtime — e.g. migrations, admin registration — and compiling them would break that). This only bites images actually built from this Dockerfile (what CI pushes to the registry that prod's Watchtower pulls from) — dev's bind-mounted `.:/app` overlays the container's `/app` with the host source tree, restoring the "deleted" `.py` files, so `runserver`/tests always see normal source regardless. PyArmor (docs/2's other proposed option) is still unused.

**Error log reporting** (`local_server/licensing/`, `cloud_server/sync/` + `cloud_server/tenants/`): a one-way, batched Bola → Ona channel, deliberately independent of the heartbeat request/response cycle — a malformed error payload must never affect the license-check/remote-command-polling critical path. `local_server/licensing/log_handler.py::DatabaseErrorLogHandler` is wired into Django's `LOGGING` (`config/settings.py`) at `ERROR` level on both the `django` and `celery` loggers (the latter requires `CELERY_WORKER_HIJACK_ROOT_LOGGER = False`, or Celery silently strips the handler in the worker/beat containers), and writes every ERROR/CRITICAL log record to the local `licensing.ErrorLog` table (lazy-imports `.models` inside `emit()` to avoid `AppRegistryNotReady`, and guards against re-entrant recursion if the DB write itself logs an error). `licensing.tasks.send_error_logs` (Celery beat, every 120s) batches unreported rows to `POST /api/sync/error-logs/` (Ona, `HeartbeatAuthentication` — a blocked/dead-license restaurant must still be able to report errors); `licensing.tasks.cleanup_error_logs` (daily) bounds local disk usage via `ERROR_LOG_RETENTION_DAYS` (reported rows) and `ERROR_LOG_MAX_UNREPORTED` (oldest-first eviction if Ona is unreachable for a long time). On Ona, `tenants.models.ErrorLog` uses the Bola-generated UUID as its own PK so retried batches are naturally deduped via `bulk_create(ignore_conflicts=True)`; `tenants/admin.py::ErrorLogAdmin` shows level/resolved badges and `mark_resolved`/`mark_unresolved` actions, and `RestaurantAdmin` surfaces an unresolved-error-count badge per restaurant. Admin-panel-only by design (no Telegram/email push yet — see Implementation status below).

### Cloud tenancy (`cloud_server/tenants/models.py`)
See `cloud_server/tenants/CLAUDE.md` (loads automatically when working under that directory): the `Restaurant`/`License`/`RestaurantStatus`/`RemoteCommand`/`ErrorLog`/`SyncedOrder*` models, the write-cheap online/offline tracking design, auto-created default licenses, and billing/renewal surfacing in the admin.

### Auth quirks
- `local_server/core/middleware.py` — `SwaggerTokenPrefixMiddleware` auto-prepends `"Token "` to `Authorization` headers that are exactly 40 chars with no space, purely to make testing via Swagger UI easier. It's appended to `MIDDLEWARE` only under `DEBUG`, so prod clients must always send the full `Token <key>` form.
- `local_server` uses `core.authentication.DeviceTokenAuthentication` (a `TokenAuthentication` subclass that additionally re-validates `X-Device-ID`) for its own POS users (see `local_server/core/CLAUDE.md` for the three different ways that token gets issued); `cloud_server`'s `sync` app uses `LicenseAuthentication`/`HeartbeatAuthentication` (keyword is also literally `'Token'`, not `'License'`, despite the class name) — don't conflate the two token systems when adding endpoints.
- Neither `LicenseAuthentication` nor `HeartbeatAuthentication` override `authenticate_header()`, so DRF coerces auth failures from 401 to 403 (see `get_authenticate_header` in DRF's `APIView.handle_exception`). Tests assert 403, not 401, for these endpoints — this is existing, intentional-by-omission behavior, not a bug to "fix" reflexively.

### Admin
Both projects use `django-jazzmin` (dark theme) as the admin skin, configured in each `config/settings.py` under `JAZZMIN_SETTINGS`/`JAZZMIN_UI_TWEAKS`.

## Implementation status vs. docs

- ✅ Full licensing/control-plane loop: activation, RS256 JWT issuance + offline verification, pre-issued sequential token batches for weeks-long offline survival (`issue_license_token_batch`/`LicenseState.pending_tokens`) plus a manual single-token offline renewal/unblock path (`ApplyOfflineTokenView`) for restaurants that exhaust the batch, daily auto-renewal, kill-switch middleware, heartbeat with real psutil metrics, online/offline tracking, remote command queue (block/unblock/force-renew/update-app/restart-refused), Cython-compiled kill-switch modules in the prod image (PyArmor still unused), Watchtower-based rollout (prod compose only) - live-tested end-to-end against a local registry (build → push → `POST /v1/update` → container recreation confirmed via a version marker). That test caught a real bug: the `containrrr/watchtower` image is stale (no update since 2023-11) and crash-loops against modern Docker Engine ("client version 1.25 is too old"), so `docker-compose.prod.yml` now pins `nickfedor/watchtower` (maintained fork) instead - unverified beyond that swap (real registry auth, `~/.docker/config.json` mount, actual restaurant hardware not tested).
- ✅ Bola → Ona error log reporting (admin-panel visibility only, no push alerting; `DatabaseErrorLogHandler` now offloads to a background thread when called from inside a running asyncio event loop, e.g. Daphne/Channels code paths, to avoid blocking it), full field-level history (`django-simple-history`) on every `local_server` model, and split-payment + manager-gated discount tracking on `Order` (see `local_server/core/CLAUDE.md`) — not yet reflected anywhere under `docs/`.
- ✅ Staff mobile-app auth foundation: PIN + device-bound login (registration codes, one-active-device enforcement, admin revoke with immediate WS disconnect), a separate TOFU + manager-approval phone/password login for waiters (`WaiterLoginView`), device identity now re-checked on *every* API/WS call (`DeviceTokenAuthentication`, not just at login), and a Channels/WebSocket real-time event channel (`ws/events/`) for POS state pushes — see `local_server/core/CLAUDE.md`. Backs the Flutter admin/manager-cashier/waiter apps; also undocumented anywhere under `docs/`.
- ✅ `TableZone` table grouping, staff attendance with geofencing (`RestaurantConfig` singleton + `Attendance` + haversine check-in/check-out), and a kitchen ticket-routing system (`Printer`/`PrintJob`/`Category.printer`/`OrderItem.is_printed`, plus a browser-based virtual-printer demo dashboard at `/kitchen/printers/`) — now including **real ESC/POS thermal-printer output over TCP 9100** (`core/escpos.py` stdlib-only renderer + `core/tasks.py` Celery send/retry/sweep + `POST /api/printers/<id>/test-print/`; live-tested on an Xprinter XP-Q80A via USB/CUPS) — see `local_server/core/CLAUDE.md`; none of this reflected under `docs/`.
- ✅ The `future-docs/` audit-response round: committed-migrations strategy, order-lifecycle hardening (`status` read-only via API, `start`/`cancel` actions, finished-order guards on every mutating action, `/api/order-items/` removed), upstream completed-order sync to Ona (see `local_server/core/CLAUDE.md`), DRF-wide throttling, prod-compose hardening (healthchecked `depends_on`, read-only root FS, cloud `db_backup`). Not yet reflected under `docs/`.
- ❌ Not yet built: downstream menu sync (Ona → Bola — `force_sync` is still a stub), admin visibility for `SyncedOrder*` rows, `MenuUpdateLog`, PyArmor obfuscation (docs/2 proposed Cython *or* PyArmor — only Cython shipped, see Licensing section), Telegram/email alerting on error reports or otherwise (admin-panel-only by design choice), `restart_services` remote command (needs docker.sock, deliberately not wired up), USB/serial transports for thermal printers (only network/TCP 9100 is wired up — USB was used once as a host-level CUPS smoke test, containers can't see it), payment/order reporting & analytics UI (sales data now accumulates on both sides, the reports themselves don't exist yet).
- Test suites pass: `local_server` → `manage.py test core licensing` (176 tests), `cloud_server` → `manage.py test sync tenants` (48 tests), both green as of 2026-07-12. They rely on the test-mode settings described in Commands above (throttling off, cache on Redis DB `/9`); tests that touch `restaurant_metrics_*` cache keys must still set/delete them explicitly (see `MarkOfflineRestaurantsTests`) since Redis state survives between runs. The deploy workflow's `test` job also runs `makemigrations --check --dry-run` — a model change without its committed migration now fails CI instead of silently breaking prod.

## Repo state

This is a git repository with a GitHub remote (`origin`, `main` is the default branch). `future-docs/` holds a full external-style code audit plus a prioritized roadmap (Uzbek; start at its `README.md` for reading order and the 🔴/🟠/🟡/🟢 severity legend, `10_yol_xaritasi.md` for the ordered plan). It was written against the pre-refactor code, and its two flagship 🔴 findings — the uncommitted-migrations strategy and `Order.status` being writable via generic PATCH — are already fixed in the current tree, so verify a finding against the code before "re-fixing" it.

There is also an unrelated **orphan** `api-docs` branch (no shared history with `main`) holding only hand-written Markdown API reference for the Flutter staff mobile apps — see its own `README.md` for the update convention (it's periodically rewritten wholesale rather than accumulating long history). **Any change to a `local_server` API endpoint must be reflected there too** (mobile-facing endpoints only — internal Bola↔Ona sync endpoints don't belong on that branch); edit it via an isolated `git worktree add <path> api-docs`, never `git checkout` directly, since `main` commonly has unrelated work in progress.

**Pushing to `main` auto-deploys `cloud_server` to production:** `.github/workflows/deploy-cloud-server.yml` triggers on any push to `main` that touches `cloud_server/**` (path-filtered so `local_server`-only changes don't trigger it), SSHes into the EC2 host, takes a best-effort `pg_dump` into `cloud_server/backups/` first, then `git reset --hard origin/main`s the checkout and runs `docker compose -f docker-compose.prod.yml up -d --build`. The job runs under the `production` GitHub environment, so the only approval gate is whatever protection rules that environment has configured on GitHub — nothing in-repo blocks the deploy. `local_server` has no equivalent *deploy* workflow (nothing SSHes into a Bola) — each restaurant's Bola updates itself via Watchtower (see Update rollout above). It does now have a *build* workflow, `release-local-server.yml`, triggered by `v*` tags (not branch pushes), which only builds+pushes the Bola image to ghcr; the actual per-restaurant rollout is still Watchtower-driven off the Ona `update_app` command.
